using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Rose2C : MonoBehaviour
{

    // Start is called before the first frame update
    void Start()
    {
        
    }

    public void EShot1()
    {
        Destroy(gameObject,0.5f);
        //StartCoroutine("Game");

    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
